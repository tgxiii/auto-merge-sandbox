name: Check conditional

on:
  pull_request:

jobs:
  conditional-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fail if FAIL file exists
        run: |
          if [ -f FAIL ]; then
            echo "FAIL file present"
            exit 1
          fi
          echo "ok"
